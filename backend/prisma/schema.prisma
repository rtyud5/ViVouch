generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Implement models:
// User, Partner, Branch, Category, Voucher, VoucherBranch,
// Cart, CartItem, Order, OrderItem, Payment,
// VoucherCode, VoucherUsageLog, Review, AuditLog, Banner, CmsPage.
