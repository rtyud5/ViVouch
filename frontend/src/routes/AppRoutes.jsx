import { BrowserRouter, Routes, Route } from "react-router-dom";

export function AppRoutes() {
  return (
    <BrowserRouter>
      <Routes>
        {/* TODO: define public/customer/partner/admin routes */}
      </Routes>
    </BrowserRouter>
  );
}
