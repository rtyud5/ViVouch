# Prompt — Bootstrap Project

You are implementing a student final project: online discount voucher e-commerce system.

Read `AGENTS.md` and docs before coding.

Task:
1. Create monorepo with `frontend/` and `backend/`.
2. Frontend: React 18 + Vite + JavaScript, TailwindCSS + DaisyUI, React Router, TanStack Query, Zustand.
3. Backend: Express + JavaScript, dotenv, cors, helmet, pino, error middleware.
4. Add `/health` endpoint.
5. Add `.env.example` files.
6. Add root README with run commands.
7. Do not implement business modules yet.

Acceptance:
- `frontend npm run dev` works.
- `backend npm run dev` works.
- `GET /health` returns success.
- Folder structure follows docs.
